using UnityEngine;

public class Carry : MonoBehaviour
{
    public void StartCarrying(INTERACTIBLE_TYPE originType, Transform originTransform)
    {
        // ... At this point you'd have already assigned the right properties in inspector.
    }
}