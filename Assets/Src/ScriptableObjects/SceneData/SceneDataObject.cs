using UnityEngine;

public class SceneDataObject : ScriptableObject
{
    /*
     Hold on to:
     - Scene name
     - Scene memory objects (spawners, item placements, mechanism states, etc), perhaps use an interface?
     - (tbc)
     */
}
